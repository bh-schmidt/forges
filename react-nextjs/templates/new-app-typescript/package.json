{
  "name": "{{ projectName }}",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack -p {{ localPort }}",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  }
}
